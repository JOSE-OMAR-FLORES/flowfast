<div>
    {{-- Header --}}
    <div class="mb-6">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-2xl font-bold text-gray-900">Partido en Vivo</h1>
                <p class="mt-1 text-sm text-gray-500">{{ $match->season->league->name }} - {{ $match->season->name }}</p>
            </div>
            <a href="{{ route('fixtures.index') }}" class="px-4 py-2 border border-gray-300 text-gray-700 hover:bg-gray-50 rounded-lg font-medium transition-colors">
                ← Volver
            </a>
        </div>
    </div>

    {{-- Alerts --}}
    @if(session()->has('success'))
        <div class="mb-6 p-4 bg-green-50 border border-green-200 text-green-800 rounded-lg">
            {{ session('success') }}
        </div>
    @endif
    @if(session()->has('error'))
        <div class="mb-6 p-4 bg-red-50 border border-red-200 text-red-800 rounded-lg">
            {{ session('error') }}
        </div>
    @endif

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {{-- Sección Principal --}}
        <div class="lg:col-span-2 space-y-6">
            {{-- Marcador --}}
            <div class="bg-gradient-to-br from-blue-600 to-indigo-700 rounded-lg shadow-lg p-8 text-white">
                <div class="flex items-center justify-between mb-4">
                    <div class="flex items-center space-x-2">
                        <span class="px-3 py-1 rounded-full text-xs font-semibold {{ $match->isLive() ? 'bg-green-500' : ($match->isFinished() ? 'bg-gray-500' : 'bg-blue-500') }}">
                            {{ ucfirst($match->status) }}
                        </span>
                        @if($match->isLive())
                            <span class="flex h-3 w-3">
                                <span class="animate-ping absolute inline-flex h-3 w-3 rounded-full bg-green-400 opacity-75"></span>
                                <span class="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span>
                            </span>
                        @endif
                    </div>
                    @if($match->venue)
                        <div class="text-sm opacity-90">📍 {{ $match->venue->name }}</div>
                    @endif
                </div>

                <div class="flex items-center justify-between">
                    {{-- Home Team --}}
                    <div class="flex-1 text-center">
                        <div class="text-2xl font-bold mb-2">{{ $match->homeTeam->name }}</div>
                        <div class="text-6xl font-extrabold">{{ $match->home_score ?? 0 }}</div>
                    </div>

                    {{-- VS --}}
                    <div class="px-6">
                        <div class="text-3xl font-bold opacity-75">VS</div>
                    </div>

                    {{-- Away Team --}}
                    <div class="flex-1 text-center">
                        <div class="text-2xl font-bold mb-2">{{ $match->awayTeam->name }}</div>
                        <div class="text-6xl font-extrabold">{{ $match->away_score ?? 0 }}</div>
                    </div>
                </div>

                {{-- Match Controls --}}
                <div class="mt-8 flex justify-center space-x-4">
                    @if($match->status === 'scheduled')
                        <button 
                            wire:click="startMatch"
                            class="px-6 py-3 bg-green-500 hover:bg-green-600 text-white rounded-lg font-semibold transition-colors shadow-lg"
                        >
                            ▶ Iniciar Partido
                        </button>
                    @elseif($match->status === 'live')
                        <button 
                            wire:click="finishMatch"
                            class="px-6 py-3 bg-red-500 hover:bg-red-600 text-white rounded-lg font-semibold transition-colors shadow-lg"
                        >
                            ⏹ Finalizar Partido
                        </button>
                    @elseif($match->status === 'finished')
                        <span class="px-6 py-3 bg-gray-600 text-white rounded-lg font-semibold">
                            ✓ Partido Finalizado
                        </span>
                    @endif
                </div>
            </div>

            {{-- Sección de Pagos (solo si el partido ha finalizado) --}}
            @if($match->status === 'finished')
                {{-- Pagos de Equipos (Income) --}}
                @if(in_array(auth()->user()->user_type, ['admin', 'league_manager', 'referee']))
                    <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-200">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="font-semibold text-gray-900">💰 Pagos de Equipos</h3>
                            <span class="text-xs text-gray-500">{{ $match->incomes->count() }} pagos</span>
                        </div>
                        
                        @if($match->incomes->count() > 0)
                            <div class="space-y-2">
                                @foreach($match->incomes as $income)
                                    <div class="flex items-center justify-between p-3 {{ $income->payment_status === 'confirmed' ? 'bg-green-50 border border-green-200' : 'bg-gray-50 border border-gray-200' }} rounded-lg">
                                        <div class="flex-1">
                                            <div class="font-medium text-gray-900 text-sm">
                                                {{ $income->team->name ?? 'Equipo' }}
                                            </div>
                                            <div class="text-sm text-gray-700 font-semibold">
                                                ${{ number_format($income->amount, 2) }}
                                            </div>
                                            <div class="text-xs mt-1">
                                                @if($income->payment_status === 'pending')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                                                        🔴 Pendiente de pago
                                                    </span>
                                                @elseif($income->payment_status === 'paid')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                        🟡 Pagado - Por confirmar
                                                    </span>
                                                @elseif($income->payment_status === 'confirmed')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                        🟢 Confirmado
                                                    </span>
                                                @endif
                                            </div>
                                        </div>
                                        
                                        @if($income->payment_status === 'paid')
                                            <button 
                                                wire:click="confirmTeamPayment({{ $income->id }})"
                                                class="ml-2 px-3 py-1.5 bg-green-600 hover:bg-green-700 text-white text-xs rounded-lg font-medium transition-colors"
                                            >
                                                ✓ Confirmar
                                            </button>
                                        @endif
                                    </div>
                                @endforeach
                            </div>
                        @else
                            <div class="text-center py-3">
                                <p class="text-sm text-gray-500">No hay pagos generados</p>
                            </div>
                        @endif
                    </div>
                @endif

                {{-- Pagos a Árbitros (Expense) - Vista para Referee --}}
                @if(auth()->user()->user_type === 'referee')
                    <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-200">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="font-semibold text-gray-900">💵 Mi Pago como Árbitro</h3>
                        </div>
                        
                        @php
                            $myPayments = $match->expenses->where('beneficiary_user_id', auth()->id());
                        @endphp
                        
                        @if($myPayments->count() > 0)
                            <div class="space-y-2">
                                @foreach($myPayments as $expense)
                                    <div class="flex items-center justify-between p-3 {{ $expense->payment_status === 'confirmed' ? 'bg-green-50 border border-green-200' : 'bg-gray-50 border border-gray-200' }} rounded-lg">
                                        <div class="flex-1">
                                            <div class="text-sm font-semibold text-gray-900">
                                                ${{ number_format($expense->amount, 2) }}
                                            </div>
                                            <div class="text-xs text-gray-600 mt-1">
                                                {{ $expense->description }}
                                            </div>
                                            <div class="text-xs mt-1">
                                                @if($expense->payment_status === 'pending')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                                                        🔴 Pendiente de aprobación
                                                    </span>
                                                @elseif($expense->payment_status === 'approved')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                        🟡 Aprobado - En proceso de pago
                                                    </span>
                                                @elseif($expense->payment_status === 'paid')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                        🟢 Pagado - Confirma recepción
                                                    </span>
                                                @elseif($expense->payment_status === 'confirmed')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                        ✅ Confirmado recibido
                                                    </span>
                                                @endif
                                            </div>
                                        </div>
                                        
                                        @if($expense->payment_status === 'paid')
                                            <button 
                                                wire:click="confirmMyPayment({{ $expense->id }})"
                                                class="ml-2 px-3 py-1.5 bg-green-600 hover:bg-green-700 text-white text-xs rounded-lg font-medium transition-colors"
                                            >
                                                ✓ Confirmar Recepción
                                            </button>
                                        @endif
                                    </div>
                                @endforeach
                            </div>
                        @else
                            <div class="text-center py-3">
                                <p class="text-sm text-gray-500">No tienes pagos asignados en este partido</p>
                            </div>
                        @endif
                    </div>
                @endif

                {{-- Pagos a Árbitros (Expense) - Vista para Admin/Manager --}}
                @if(in_array(auth()->user()->user_type, ['admin', 'league_manager']))
                    <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-200">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="font-semibold text-gray-900">💵 Pagos a Árbitros</h3>
                            <span class="text-xs text-gray-500">{{ $match->expenses->count() }} pagos</span>
                        </div>
                        
                        @if($match->expenses->count() > 0)
                            <div class="space-y-2">
                                @foreach($match->expenses as $expense)
                                    <div class="flex items-center justify-between p-3 {{ $expense->payment_status === 'confirmed' ? 'bg-green-50 border border-green-200' : 'bg-gray-50 border border-gray-200' }} rounded-lg">
                                        <div class="flex-1">
                                            <div class="font-medium text-gray-900 text-sm">
                                                {{ $expense->referee->first_name ?? '' }} {{ $expense->referee->last_name ?? '' }}
                                            </div>
                                            <div class="text-sm text-gray-700 font-semibold">
                                                ${{ number_format($expense->amount, 2) }}
                                            </div>
                                            <div class="text-xs text-gray-600 mt-0.5">
                                                {{ $expense->description }}
                                            </div>
                                            <div class="text-xs mt-1">
                                                @if($expense->payment_status === 'pending')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                                                        🔴 Pendiente de aprobación
                                                    </span>
                                                @elseif($expense->payment_status === 'approved')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                        🟡 Aprobado - Pendiente de pagar
                                                    </span>
                                                @elseif($expense->payment_status === 'paid')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                        🟢 Pagado - Esperando confirmación del árbitro
                                                    </span>
                                                @elseif($expense->payment_status === 'confirmed')
                                                    <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                        ✅ Confirmado por árbitro
                                                    </span>
                                                @endif
                                            </div>
                                        </div>
                                        
                                        <div class="ml-2 flex flex-col gap-1">
                                            @if($expense->payment_status === 'pending')
                                                <button 
                                                    wire:click="approveRefereePayment({{ $expense->id }})"
                                                    class="px-3 py-1.5 bg-blue-600 hover:bg-blue-700 text-white text-xs rounded-lg font-medium transition-colors whitespace-nowrap"
                                                >
                                                    ✓ Aprobar
                                                </button>
                                            @elseif($expense->payment_status === 'approved')
                                                <button 
                                                    wire:click="markAsPaid({{ $expense->id }})"
                                                    class="px-3 py-1.5 bg-green-600 hover:bg-green-700 text-white text-xs rounded-lg font-medium transition-colors whitespace-nowrap"
                                                >
                                                    💵 Marcar Pagado
                                                </button>
                                            @endif
                                        </div>
                                    </div>
                                @endforeach
                            </div>
                        @else
                            <div class="text-center py-3">
                                <p class="text-sm text-gray-500">No hay pagos generados</p>
                            </div>
                        @endif
                    </div>
                @endif
            @endif

            {{-- Eventos del Partido --}}ext-gray-500">{{ $match->season->league->name }} - {{ $match->season->name }}</p>
            </div>
            <a href="{{ route('fixtures.index') }}" class="px-4 py-2 border border-gray-300 text-gray-700 hover:bg-gray-50 rounded-lg font-medium transition-colors">
                ← Volver
            </a>
        </div>
    </div>

    {{-- Alerts --}}
    @if(session()->has('success'))
        <div class="mb-6 p-4 bg-green-50 border border-green-200 text-green-800 rounded-lg">
            {{ session('success') }}
        </div>
    @endif
    @if(session()->has('error'))
        <div class="mb-6 p-4 bg-red-50 border border-red-200 text-red-800 rounded-lg">
            {{ session('error') }}
        </div>
    @endif

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {{-- Sección Principal --}}
        <div class="lg:col-span-2 space-y-6">
            {{-- Marcador --}}
            <div class="bg-gradient-to-br from-blue-600 to-indigo-700 rounded-lg shadow-lg p-8 text-white">
                <div class="flex items-center justify-between mb-4">
                    <div class="flex items-center space-x-2">
                        <span class="px-3 py-1 rounded-full text-xs font-semibold {{ $match->isLive() ? 'bg-green-500' : ($match->isFinished() ? 'bg-gray-500' : 'bg-blue-500') }}">
                            {{ ucfirst($match->status) }}
                        </span>
                        @if($match->isLive())
                            <span class="flex h-3 w-3">
                                <span class="animate-ping absolute inline-flex h-3 w-3 rounded-full bg-green-400 opacity-75"></span>
                                <span class="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span>
                            </span>
                        @endif
                    </div>
                    @if($match->venue)
                        <div class="text-sm opacity-90">📍 {{ $match->venue->name }}</div>
                    @endif
                </div>

                <div class="flex items-center justify-between">
                    {{-- Home Team --}}
                    <div class="flex-1 text-center">
                        <div class="text-2xl font-bold mb-2">{{ $match->homeTeam->name }}</div>
                        <div class="text-6xl font-extrabold">{{ $match->home_score ?? 0 }}</div>
                    </div>

                    {{-- VS --}}
                    <div class="px-6">
                        <div class="text-3xl font-bold opacity-75">VS</div>
                    </div>

                    {{-- Away Team --}}
                    <div class="flex-1 text-center">
                        <div class="text-2xl font-bold mb-2">{{ $match->awayTeam->name }}</div>
                        <div class="text-6xl font-extrabold">{{ $match->away_score ?? 0 }}</div>
                    </div>
                </div>

                @if($match->status === 'in_progress' || $match->status === 'completed')
                    <div class="mt-6 text-center text-sm opacity-90">
                        Estado: {{ ucfirst($match->status) }}
                        {{-- Fixture no tiene started_at/finished_at/duration_minutes --}}
                    </div>
                @endif

                {{-- Controles del Partido --}}
                <div class="mt-8 flex gap-3 justify-center">
                    @if($match->canStart())
                        <button 
                            wire:click="startMatch"
                            wire:confirm="¿Iniciar el partido?"
                            class="px-6 py-3 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-lg transition-colors"
                        >
                            ▶️ Iniciar Partido
                        </button>
                    @endif

                    @if($match->canFinish())
                        <button 
                            wire:click="finishMatch"
                            wire:confirm="¿Finalizar el partido?"
                            class="px-6 py-3 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-lg transition-colors"
                        >
                            ⏹️ Finalizar Partido
                        </button>
                    @endif
                </div>
            </div>

            {{-- Botones de Eventos --}}
            @if($match->isLive())
                <div class="grid grid-cols-2 gap-4">
                    {{-- Eventos Home Team --}}
                    <div class="bg-white rounded-lg shadow-sm p-4">
                        <h3 class="font-semibold text-gray-900 mb-3">{{ $match->homeTeam->name }}</h3>
                        <div class="space-y-2">
                            <button wire:click="openEventForm('goal', {{ $match->home_team_id }})" class="w-full px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                                ⚽ Gol
                            </button>
                            <button wire:click="openEventForm('yellow_card', {{ $match->home_team_id }})" class="w-full px-4 py-2 bg-yellow-400 hover:bg-yellow-500 text-gray-900 rounded-lg font-medium transition-colors">
                                🟨 Amarilla
                            </button>
                            <button wire:click="openEventForm('red_card', {{ $match->home_team_id }})" class="w-full px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium transition-colors">
                                🟥 Roja
                            </button>
                            <button wire:click="openEventForm('substitution', {{ $match->home_team_id }})" class="w-full px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors">
                                🔄 Cambio
                            </button>
                        </div>
                    </div>

                    {{-- Eventos Away Team --}}
                    <div class="bg-white rounded-lg shadow-sm p-4">
                        <h3 class="font-semibold text-gray-900 mb-3">{{ $match->awayTeam->name }}</h3>
                        <div class="space-y-2">
                            <button wire:click="openEventForm('goal', {{ $match->away_team_id }})" class="w-full px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg font-medium transition-colors">
                                ⚽ Gol
                            </button>
                            <button wire:click="openEventForm('yellow_card', {{ $match->away_team_id }})" class="w-full px-4 py-2 bg-yellow-400 hover:bg-yellow-500 text-gray-900 rounded-lg font-medium transition-colors">
                                🟨 Amarilla
                            </button>
                            <button wire:click="openEventForm('red_card', {{ $match->away_team_id }})" class="w-full px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium transition-colors">
                                🟥 Roja
                            </button>
                            <button wire:click="openEventForm('substitution', {{ $match->away_team_id }})" class="w-full px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors">
                                🔄 Cambio
                            </button>
                        </div>
                    </div>
                </div>
            @endif

            {{-- Timeline de Eventos --}}
            <div class="bg-white rounded-lg shadow-sm p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">📋 Eventos del Partido</h3>
                
                @if($events->count() > 0)
                    <div class="space-y-3">
                        @foreach($events as $event)
                            <div class="flex items-center justify-between p-3 bg-gray-50 hover:bg-gray-100 rounded-lg transition-colors">
                                <div class="flex items-center space-x-4">
                                    <div class="text-2xl">{{ $event->emoji }}</div>
                                    <div>
                                        <div class="font-medium text-gray-900">
                                            {{ $event->label }}
                                            @if($event->isSubstitution())
                                                (Sale: {{ $event->player->full_name }})
                                            @else
                                                - {{ $event->player->full_name ?? 'N/A' }}
                                            @endif
                                        </div>
                                        <div class="text-sm text-gray-600">
                                            {{ $event->team->name }} • Minuto {{ $event->full_minute }}'
                                            @if($event->description)
                                                <br>{{ $event->description }}
                                            @endif
                                        </div>
                                    </div>
                                </div>
                                <div class="flex items-center space-x-2">
                                    <span class="px-3 py-1 bg-blue-100 text-blue-800 text-xs font-semibold rounded-full">
                                        {{ $event->full_minute }}'
                                    </span>
                                    @if($match->isLive())
                                        <button 
                                            wire:click="deleteEvent({{ $event->id }})"
                                            wire:confirm="¿Eliminar este evento?"
                                            class="px-2 py-1 text-red-600 hover:bg-red-50 rounded transition-colors"
                                        >
                                            ✕
                                        </button>
                                    @endif
                                </div>
                            </div>
                        @endforeach
                    </div>
                @else
                    <div class="text-center py-8 text-gray-500">
                        <div class="text-4xl mb-2">📝</div>
                        <p>No hay eventos registrados todavía</p>
                    </div>
                @endif
            </div>
        </div>

        {{-- Sidebar --}}
        <div class="space-y-6">
            {{-- Árbitros Asignados --}}
            <div class="bg-white rounded-lg shadow-sm p-4">
                <div class="flex items-center justify-between mb-3">
                    <h3 class="font-semibold text-gray-900">🎽 Árbitros</h3>
                    @if($match->status === 'scheduled')
                        <button 
                            wire:click="openRefereeModal"
                            class="px-3 py-1 bg-blue-600 hover:bg-blue-700 text-white text-sm rounded-lg font-medium transition-colors"
                        >
                            + Asignar
                        </button>
                    @endif
                </div>
                
                @if($match->referees->count() > 0)
                    <div class="space-y-2">
                        @foreach($match->referees as $referee)
                            <div class="flex items-center justify-between p-2 bg-gray-50 rounded-lg">
                                <div>
                                    <div class="font-medium text-gray-900 text-sm">
                                        {{ $referee->userable->first_name ?? '' }} {{ $referee->userable->last_name ?? '' }}
                                    </div>
                                    <div class="text-xs text-gray-600">
                                        @if($referee->pivot->referee_type === 'main')
                                            🟢 Principal
                                        @elseif($referee->pivot->referee_type === 'assistant')
                                            🔵 Asistente
                                        @else
                                            🟡 Cuarto árbitro
                                        @endif
                                    </div>
                                </div>
                                @if($match->status === 'scheduled')
                                    <button 
                                        wire:click="removeReferee({{ $referee->id }})"
                                        wire:confirm="¿Remover este árbitro?"
                                        class="text-red-600 hover:bg-red-50 px-2 py-1 rounded transition-colors text-sm"
                                    >
                                        ✕
                                    </button>
                                @endif
                            </div>
                        @endforeach
                    </div>
                @else
                    <div class="text-center py-4">
                        <div class="text-2xl mb-2">👤</div>
                        <p class="text-sm text-gray-500">No hay árbitros asignados</p>
                        @if($match->status === 'scheduled')
                            <p class="text-xs text-red-600 mt-1">⚠️ Debes asignar al menos un árbitro</p>
                        @endif
                    </div>
                @endif
            </div>

            {{-- Info del Partido --}}
            <div class="bg-white rounded-lg shadow-sm p-4">
                <h3 class="font-semibold text-gray-900 mb-3">ℹ️ Información</h3>
                <div class="space-y-2 text-sm">
                    <div>
                        <span class="text-gray-600">Jornada:</span>
                        <span class="font-medium">Jornada {{ $match->round_number ?? 'N/A' }}</span>
                    </div>
                    <div>
                        <span class="text-gray-600">Programado:</span>
                        <span class="font-medium">{{ $match->match_date->format('d/m/Y') }} {{ $match->match_time }}</span>
                    </div>
                    @if($match->referee)
                        <div>
                            <span class="text-gray-600">Árbitro:</span>
                            <span class="font-medium">{{ $match->referee->name ?? 'N/A' }}</span>
                        </div>
                    @endif
                </div>
            </div>

            {{-- Jugadores Home --}}
            <div class="bg-white rounded-lg shadow-sm p-4">
                <h3 class="font-semibold text-gray-900 mb-3">👥 {{ $match->homeTeam->name }}</h3>
                <div class="space-y-1 text-sm max-h-60 overflow-y-auto">
                    @foreach($homeTeamPlayers as $player)
                        <div class="flex items-center space-x-2 py-1">
                            <span class="w-8 h-8 flex items-center justify-center bg-blue-100 text-blue-800 rounded-full text-xs font-bold">
                                {{ $player->jersey_number ?? '-' }}
                            </span>
                            <span class="text-gray-900">{{ $player->full_name }}</span>
                        </div>
                    @endforeach
                </div>
            </div>

            {{-- Jugadores Away --}}
            <div class="bg-white rounded-lg shadow-sm p-4">
                <h3 class="font-semibold text-gray-900 mb-3">👥 {{ $match->awayTeam->name }}</h3>
                <div class="space-y-1 text-sm max-h-60 overflow-y-auto">
                    @foreach($awayTeamPlayers as $player)
                        <div class="flex items-center space-x-2 py-1">
                            <span class="w-8 h-8 flex items-center justify-center bg-red-100 text-red-800 rounded-full text-xs font-bold">
                                {{ $player->jersey_number ?? '-' }}
                            </span>
                            <span class="text-gray-900">{{ $player->full_name }}</span>
                        </div>
                    @endforeach
                </div>
            </div>
        </div>
    </div>

    {{-- Modal de Evento --}}
    @if($showEventForm)
        <div class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50" wire:click.self="closeEventForm">
            <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-md">
                <h3 class="text-xl font-bold text-gray-900 mb-4">
                    {{ $eventTypes[$eventType] ?? 'Evento' }}
                </h3>

                <form wire:submit="addEvent" class="space-y-4">
                    @if($eventType === 'substitution')
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Jugador que Sale *</label>
                            <select wire:model="playerOutId" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                <option value="">Selecciona jugador</option>
                                @foreach($teamId == $match->home_team_id ? $homeTeamPlayers : $awayTeamPlayers as $player)
                                    <option value="{{ $player->id }}">
                                        #{{ $player->jersey_number ?? '-' }} - {{ $player->full_name }}
                                    </option>
                                @endforeach
                            </select>
                            @error('playerOutId') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Jugador que Entra *</label>
                            <select wire:model="playerInId" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                <option value="">Selecciona jugador</option>
                                @foreach($teamId == $match->home_team_id ? $homeTeamPlayers : $awayTeamPlayers as $player)
                                    <option value="{{ $player->id }}">
                                        #{{ $player->jersey_number ?? '-' }} - {{ $player->full_name }}
                                    </option>
                                @endforeach
                            </select>
                            @error('playerInId') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                        </div>
                    @else
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Jugador *</label>
                            <select wire:model="playerId" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                <option value="">Selecciona jugador</option>
                                @foreach($teamId == $match->home_team_id ? $homeTeamPlayers : $awayTeamPlayers as $player)
                                    <option value="{{ $player->id }}">
                                        #{{ $player->jersey_number ?? '-' }} - {{ $player->full_name }}
                                    </option>
                                @endforeach
                            </select>
                            @error('playerId') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                        </div>
                    @endif

                    <div class="grid grid-cols-2 gap-3">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Minuto *</label>
                            <input 
                                type="number" 
                                wire:model="minute"
                                min="0"
                                max="150"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                placeholder="0"
                            >
                            @error('minute') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Añadido</label>
                            <input 
                                type="number" 
                                wire:model="extraTime"
                                min="0"
                                max="20"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                placeholder="0"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Descripción (opcional)</label>
                        <textarea 
                            wire:model="description"
                            rows="2"
                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                            placeholder="Detalles adicionales..."
                        ></textarea>
                    </div>

                    <div class="flex gap-3 pt-4">
                        <button 
                            type="submit"
                            class="flex-1 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors"
                        >
                            Registrar
                        </button>
                        <button 
                            type="button"
                            wire:click="closeEventForm"
                            class="px-6 py-3 border border-gray-300 text-gray-700 hover:bg-gray-50 font-semibold rounded-lg transition-colors"
                        >
                            Cancelar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    @endif

    {{-- Modal de Asignar Árbitro --}}
    @if($showRefereeModal)
        <div class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50" wire:click.self="closeRefereeModal">
            <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-md">
                <h3 class="text-xl font-bold text-gray-900 mb-4">
                    🎽 Asignar Árbitro
                </h3>

                <form wire:submit="addReferee" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Árbitro *</label>
                        <select wire:model="selectedRefereeId" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                            <option value="">Selecciona un árbitro</option>
                            @foreach($availableReferees as $ref)
                                <option value="{{ $ref['id'] }}">
                                    {{ $ref['full_name'] }}
                                </option>
                            @endforeach
                        </select>
                        @error('selectedRefereeId') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                        
                        @if(count($availableReferees) === 0)
                            <p class="mt-2 text-sm text-red-600">⚠️ No hay árbitros asignados a esta liga. Primero debes invitar árbitros a la liga.</p>
                        @endif
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Tipo de Árbitro *</label>
                        <select wire:model="selectedRefereeType" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                            <option value="main">🟢 Principal</option>
                            <option value="assistant">🔵 Asistente</option>
                            <option value="fourth_official">🟡 Cuarto Árbitro</option>
                        </select>
                        @error('selectedRefereeType') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                    </div>

                    <div class="flex gap-3 pt-4">
                        <button 
                            type="submit"
                            class="flex-1 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors"
                        >
                            Asignar
                        </button>
                        <button 
                            type="button"
                            wire:click="closeRefereeModal"
                            class="px-6 py-3 border border-gray-300 text-gray-700 hover:bg-gray-50 font-semibold rounded-lg transition-colors"
                        >
                            Cancelar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    @endif
</div>
